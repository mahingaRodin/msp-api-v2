# Load test results

Automated results are produced by GitHub Actions on the **`staging`** branch (`Staging — tests and load gate` workflow). Open the **Load gate** job logs for k6 metrics (p95, fail rate, VUs).

Manual local runs: see [`../load-test/README.md`](../load-test/README.md).

Release flow: [`../BRANCHING.md`](../BRANCHING.md).
